enum SurveyStatusType { requestSurvey, surveyPlanned, surveyStarted, surveyClosed, surveyCancelled }

enum CacheTypes {
  inspectionInfoBox,
  imageInfoBox,
  imageListInfoBox,
  userInfoBox,
}

enum InspectionStatusType { complete, inProgress }

int validExcelColumnsToRead = 7;
int maxCommentCharacters = 4000;
int maxNameCharacters = 200;
int maxSnackBarDisplayTimeInMilliSec = 800;
